#include <stdio.h>
#include <stdlib.h>
int main(){
    int n;
    //reading in format octal
    scanf("%o",&n);
    //reading in format hexadecimal
    scanf("%x",&n);
    scanf("%X",&n);
    system("pause");
    return 0;
}
