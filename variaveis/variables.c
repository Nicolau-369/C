#include <stdio.h>
#include <stdlib.h>
int main(){
    int x;//declares the variable but does not define the value
    printf("x = %d\n",x);
    x = 5;//sets the value of x to be 5
    system("pause");
    return 0;
}
