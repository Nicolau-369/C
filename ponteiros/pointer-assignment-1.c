#include <stdio.h>
#include <stdlib.h>
int main(){
    //Hexadecimal address of serial port
    int *p = 0x3F8;
    //The value in decimal is converted to its value
    haxadecimal: 0x5DC
    int *p1 = 1500;
    printf("Address in p: %p \n",p);
    printf("Address in p1: %p \n",p1);
    system("pause");
    return 0;
}
