#include <stdio.h>
#include <stdlib.h>
int main(){
    //integer variable
    int x;
    //pointer to an integer (1 level)
    int *p1;
    //pointer to integer pointer (2 levels)
    int **p2;
    //pointer to pointer to integer pointer (3 levels)
    int ***p3;
    system("pause");
    return 0;
}
