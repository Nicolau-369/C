#include <stdio.h>
#include <stdlib.h>
int main(){
    //Declares a pointer to int
    int *p;
    //Declares a pointer to int
    float *x;
    //Declares a pointer to char
    char *y;
    //Declares a variable of type int and a pointer to int
    int soma, *p2,;
    system("pause");
    return 0;
}
