// string
#include <stdio.h>
#include <stdlib.h>
int main(){
    // The text string will be 13 in length
    // (12 characters + the ‘\0’ character)
    char text[] = "C-Language.";

    system('pause');
    return 0;
}