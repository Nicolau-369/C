#include <stdio.h>
#include <stdlib.h>
enum escapes {setback='\b', tab='\t', newline='\n'};
int main(){
    enum escapes and = newline;
    printf("Test %c of %c written\n",e,e);
    e = tab;
    printf("Test %c of %c written\n",e,e);
    system("pause");
    return 0;
}
