#include <stdio.h>
#include <stdlib.h>
#include <math.h>
void print _ log(float x){
    if(x <= 0)
        return;//ends the function
    printf("Log = %f\n",log(x));
}
int main(){
    float x;
    printf("Enter x: ");
    scanf("%f", &f);
    print _ log(x);
    system("pause");
    return 0;
}
